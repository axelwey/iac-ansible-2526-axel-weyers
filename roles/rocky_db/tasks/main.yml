---
- name: Installeer mariadb-server
  ansible.builtin.dnf:
    name: mariadb-server
    state: present

- name: Zorg dat mariadb enabled en gestart is
  ansible.builtin.service:
    name: mariadb
    enabled: true
    state: started
