---
- name: Installeer qemu-guest-agent
  ansible.builtin.dnf:
    name: qemu-guest-agent
    state: present

- name: Zorg dat qemu-guest-agent enabled en gestart is
  ansible.builtin.service:
    name: qemu-guest-agent
    enabled: true
    state: started
